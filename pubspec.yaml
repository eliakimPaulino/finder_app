name: finder_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  crypto: ^3.0.6
  jaguar_jwt: ^3.0.0
  uuid: ^4.5.1
  dotenv: ^4.2.0
  flutter_native_splash: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/data/components/components.json
    - assets/data/pep/pep_440_rev6.json
    - assets/images/
    - assets/images/components/
    - assets/images/tools/
    - assets/images/tools/posicionador/
